/**
 * Method Decleration 
 * Function that is a property of an object
 * Can be called on the object with . i.e. accessing property of object
 * Good for OOP 
*/

const calculator = {
    add: function(a, b) {
        return a + b;
    }
}