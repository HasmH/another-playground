/**
 * Function Expression
 * An anonymous function assigned to variable 'add'
 * Usage:
 * Pass down as argument to another function
 * Store in variable for later use
 */

const add = function(a, b) {
    return a + b;
}